import Typography from '@material-ui/core/Typography'
const ParagraphRenderer = (node, children) => <Typography color='textPrimary' paragraph>{children}</Typography>

export default ParagraphRenderer
